<UserControl x:Class="OasisEditor.Views.DocumentEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OasisEditor"
             xmlns:views="clr-namespace:OasisEditor.Views"
             xmlns:cabinetViews="clr-namespace:OasisEditor.Features.CabinetEditor.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="700">
    <UserControl.Resources>
        <DataTemplate x:Key="MachineAssetChoiceTemplate">
            <TextBlock Text="{Binding DisplayName}" />
        </DataTemplate>
    </UserControl.Resources>
    <Grid Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,3"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   Text="{Binding FilePath}" />

        <Grid Grid.Row="1">
            <Grid MinHeight="280"
                  ClipToBounds="True"
                  Background="{DynamicResource PanelBackgroundBrush}">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Document.DocumentType}" Value="{x:Static local:EditorDocumentType.Panel2D}">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <views:SkiaPanel2DEditView />
            </Grid>

            <Grid MinHeight="280"
                  ClipToBounds="True"
                  Background="{DynamicResource PanelBackgroundBrush}">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Document.DocumentType}" Value="{x:Static local:EditorDocumentType.Face}">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <views:FaceWorkspaceView />
            </Grid>

            <Grid MinHeight="280"
                  ClipToBounds="True"
                  Background="{DynamicResource PanelBackgroundBrush}">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasCabinetViewer}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <cabinetViews:CabinetModelDocumentView />
            </Grid>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ScrollViewer.Style>
                    <Style TargetType="ScrollViewer"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding Document.DocumentType}" Value="{x:Static local:EditorDocumentType.Machine}"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style>
                </ScrollViewer.Style>
                <StackPanel Margin="16" MaxWidth="900" HorizontalAlignment="Left">
                    <TextBlock Text="Machine Composition" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,16"/>
                    <TextBlock Text="Display name" Foreground="{DynamicResource TextSecondaryBrush}"/><TextBox Text="{Binding MachineDisplayName, UpdateSourceTrigger=LostFocus}" Margin="0,3,0,12"/>
                    <TextBlock Text="Cabinet" Foreground="{DynamicResource TextSecondaryBrush}"/><Grid Margin="0,3,0,12"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><ComboBox ItemsSource="{Binding MachineCabinetChoices}" ItemTemplate="{StaticResource MachineAssetChoiceTemplate}" SelectedItem="{Binding SelectedMachineCabinetChoice, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/><Button Grid.Column="1" Content="Open" Command="{Binding OpenSelectedCabinetAssetCommand}" ToolTip="Open Asset" Margin="6,0,0,0" Padding="8,2"/></Grid>
                    <TextBlock Text="Emulation platform" Foreground="{DynamicResource TextSecondaryBrush}"/><ComboBox ItemsSource="{Binding MachinePlatforms}" SelectedItem="{Binding MachinePlatform}" Margin="0,3,0,16"/>
                    <TextBlock Text="Face Targets" FontWeight="SemiBold"/><ItemsControl ItemsSource="{Binding MachineSurfaceAssignmentRows}" Margin="0,4,0,16"><ItemsControl.ItemTemplate><DataTemplate><Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="240"/><ColumnDefinition Width="300"/></Grid.ColumnDefinitions><TextBlock Text="{Binding DisplayName}"/><TextBlock Grid.Column="1" Text="{Binding TargetId}" Foreground="{DynamicResource TextSecondaryBrush}"/><ComboBox Grid.Column="2" ItemsSource="{Binding Choices}" ItemTemplate="{StaticResource MachineAssetChoiceTemplate}" SelectedValuePath="AssetPath" SelectedValue="{Binding SelectedAssetPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/></Grid></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                    <TextBlock Text="Machine Reels" FontWeight="SemiBold"/><ItemsControl ItemsSource="{Binding MachineReelAssignmentRows}" Margin="0,4,0,16"><ItemsControl.ItemTemplate><DataTemplate><Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition Width="240"/><ColumnDefinition Width="300"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="{Binding DisplayName}"/><ComboBox Grid.Column="1" ItemsSource="{Binding Choices}" ItemTemplate="{StaticResource MachineAssetChoiceTemplate}" SelectedItem="{Binding SelectedChoice, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/><Button Grid.Column="2" Content="Open" Command="{Binding OpenSelectedAssetCommand}" ToolTip="Open Asset" Margin="6,0,0,0" Padding="8,2"/></Grid></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                    <TextBlock Text="Inputs" FontWeight="SemiBold"/><TextBlock Text="{Binding MachineInputs.Count, StringFormat={}{0} input definitions}" Margin="0,4,0,0"/>
                </StackPanel>
            </ScrollViewer>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ScrollViewer.Style><Style TargetType="ScrollViewer"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding Document.DocumentType}" Value="{x:Static local:EditorDocumentType.Reel}"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></ScrollViewer.Style>
                <StackPanel Margin="16" MaxWidth="520" HorizontalAlignment="Left">
                    <TextBlock Text="Reel" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,16"/>
                    <TextBlock Text="Display Name" Foreground="{DynamicResource TextSecondaryBrush}"/><TextBox Text="{Binding ReelDisplayName, UpdateSourceTrigger=LostFocus}" Margin="0,3,0,12"/>
                    <TextBlock Text="Diameter (mm)" Foreground="{DynamicResource TextSecondaryBrush}"/><TextBox Text="{Binding ReelDiameterMm, UpdateSourceTrigger=LostFocus}" Margin="0,3,0,12"/>
                    <TextBlock Text="Width (mm)" Foreground="{DynamicResource TextSecondaryBrush}"/><TextBox Text="{Binding ReelWidthMm, UpdateSourceTrigger=LostFocus}" Margin="0,3,0,12"/>
                </StackPanel>
            </ScrollViewer>

            <Border Padding="10"
                    CornerRadius="4"
                    Background="{DynamicResource SelectionBrush}"
                    BorderBrush="{DynamicResource BorderSubtleBrush}"
                    BorderThickness="1">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Document.DocumentType}" Value="{x:Static local:EditorDocumentType.Panel2D}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Document.DocumentType}" Value="{x:Static local:EditorDocumentType.Face}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Document.DocumentType}" Value="{x:Static local:EditorDocumentType.Machine}"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger>
                            <DataTrigger Binding="{Binding Document.DocumentType}" Value="{x:Static local:EditorDocumentType.Reel}"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger>
                            <DataTrigger Binding="{Binding HasCabinetViewer}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock TextWrapping="Wrap" Text="{Binding ContentSummary}" />
            </Border>
        </Grid>
    </Grid>
</UserControl>
