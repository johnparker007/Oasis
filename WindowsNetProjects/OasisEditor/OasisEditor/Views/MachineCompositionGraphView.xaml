<UserControl x:Class="OasisEditor.Views.MachineCompositionGraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource WorkspaceBackgroundBrush}">
    <Border Background="{DynamicResource WorkspaceBackgroundBrush}" BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="0">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{DynamicResource ToolBarBackgroundBrush}" BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="0,0,0,1" Padding="6,4">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left" Text="Machine Overview" FontSize="16" FontWeight="SemiBold" Margin="4,0,14,0" VerticalAlignment="Center"/>
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <Button Content="Fit to Content" Click="Fit_Click" Padding="8,2"/>
                    <Button Content="Actual Size" Click="Reset_Click" Padding="8,2" Margin="4,0,0,0"/>
                    <Border Width="1" Margin="10,1" Background="{DynamicResource BorderSubtleBrush}"/>
                    <TextBlock Text="Zoom:" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ZoomLabel" Text="100%" Margin="4,0,0,0" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
            </DockPanel>
        </Border>
        <Border Grid.Row="1" x:Name="Viewport" ClipToBounds="True" Background="{DynamicResource WorkspaceBackgroundBrush}"
                MouseWheel="Viewport_MouseWheel" MouseDown="Viewport_MouseDown" MouseMove="Viewport_MouseMove" MouseUp="Viewport_MouseUp"
                SizeChanged="Viewport_SizeChanged">
            <Canvas x:Name="GraphCanvas" RenderTransformOrigin="0,0"/>
        </Border>
        <Expander Grid.Row="2" Margin="8,4,8,8" Style="{StaticResource OasisDiagnosticsExpanderStyle}" Header="{Binding MachineCompositionGraph.DiagnosticsSummary}">
            <ItemsControl ItemsSource="{Binding MachineCompositionGraph.Graph.Diagnostics}">
                <ItemsControl.ItemTemplate><DataTemplate><TextBlock Margin="16,2,4,2" Text="{Binding Message}" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}"/></DataTemplate></ItemsControl.ItemTemplate>
            </ItemsControl>
        </Expander>
    </Grid>
    </Border>
</UserControl>
