<UserControl x:Class="OasisEditor.Views.EditorViewportStatusBar"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Height="26" MinWidth="300">
 <UserControl.Resources>
  <!-- The application ComboBox template only presents SelectionBoxItem and has no
       PART_EditableTextBox. This local template preserves editable zoom text. -->
  <Style x:Key="ViewportZoomComboBoxStyle" TargetType="ComboBox">
   <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}"/>
   <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
   <Setter Property="BorderBrush" Value="{DynamicResource BorderSubtleBrush}"/>
   <Setter Property="Template">
    <Setter.Value>
     <ControlTemplate TargetType="ComboBox">
      <Grid>
       <Border x:Name="ControlBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"/>
       <Grid Margin="1">
        <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="24"/></Grid.ColumnDefinitions>
        <TextBox x:Name="PART_EditableTextBox" Grid.Column="0" Margin="6,0,2,0"
                 Padding="0" HorizontalContentAlignment="Left" VerticalContentAlignment="Center"
                 Background="Transparent" Foreground="{TemplateBinding Foreground}" BorderThickness="0"
                 CaretBrush="{DynamicResource TextPrimaryBrush}" SelectionBrush="{DynamicResource SelectionBrush}"
                 IsReadOnly="{TemplateBinding IsReadOnly}"/>
        <ToggleButton x:Name="DropDownToggle" Grid.Column="1" Focusable="False" ClickMode="Press"
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                      Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1,0,0,0">
         <ToggleButton.Template>
          <ControlTemplate TargetType="ToggleButton">
           <Border x:Name="ArrowBackground" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                   Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
            <Path Width="8" Height="4" HorizontalAlignment="Center" VerticalAlignment="Center"
                  IsHitTestVisible="False" Fill="{DynamicResource TextPrimaryBrush}" Data="M 0 0 L 4 4 L 8 0 Z"/>
           </Border>
           <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ArrowBackground" Property="Background" Value="{DynamicResource ControlHoverBrush}"/></Trigger>
           </ControlTemplate.Triggers>
          </ControlTemplate>
         </ToggleButton.Template>
        </ToggleButton>
       </Grid>
       <Popup x:Name="PART_Popup" Placement="Bottom" AllowsTransparency="False" Focusable="False"
              IsOpen="{TemplateBinding IsDropDownOpen}" PopupAnimation="Slide">
        <Grid MinWidth="{TemplateBinding ActualWidth}">
         <Border Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="1"/>
         <ScrollViewer SnapsToDevicePixels="True"><ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/></ScrollViewer>
        </Grid>
       </Popup>
      </Grid>
      <ControlTemplate.Triggers>
       <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ControlBorder" Property="BorderBrush" Value="{DynamicResource BorderStrongBrush}"/></Trigger>
       <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter TargetName="ControlBorder" Property="BorderBrush" Value="{DynamicResource BorderStrongBrush}"/></Trigger>
       <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.65"/></Trigger>
      </ControlTemplate.Triggers>
     </ControlTemplate>
    </Setter.Value>
   </Setter>
  </Style>
 </UserControl.Resources>
 <Border Background="{DynamicResource ToolBarBackgroundBrush}" BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="0,1,0,0" Padding="8,2">
  <Grid>
   <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
   <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" Text="{Binding ContentDimensions, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
   <TextBlock Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" Text="{Binding PointerCoordinates, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
   <StackPanel Grid.Column="2" Orientation="Horizontal">
    <Button MinWidth="38" Padding="7,0" Content="Fit" ToolTip="Fit content in viewport" Click="OnFit"/>
    <ComboBox x:Name="ZoomBox" Margin="5,0,0,0" Width="100" Height="22" IsEditable="True"
              Style="{StaticResource ViewportZoomComboBoxStyle}" ToolTip="{Binding ZoomToolTip, RelativeSource={RelativeSource AncestorType=UserControl}}"
              IsTextSearchEnabled="False" PreviewKeyDown="OnZoomKeyDown" DropDownClosed="OnDropDownClosed">
     <ComboBoxItem Content="6.25%" Tag="0.0625"/><ComboBoxItem Content="12.5%" Tag="0.125"/>
     <ComboBoxItem Content="25%" Tag="0.25"/><ComboBoxItem Content="50%" Tag="0.5"/>
     <ComboBoxItem Content="100%" Tag="1"/><ComboBoxItem Content="200%" Tag="2"/>
     <ComboBoxItem Content="400%" Tag="4"/><ComboBoxItem Content="800%" Tag="8"/>
     <ComboBoxItem Content="1600%" Tag="16"/><ComboBoxItem Content="3200%" Tag="32"/><ComboBoxItem Content="6400%" Tag="64"/>
    </ComboBox>
   </StackPanel>
  </Grid>
 </Border>
</UserControl>
