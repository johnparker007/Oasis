<UserControl x:Class="OasisEditor.Views.PreferencesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="560"
             d:DesignWidth="620">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="170" />
            <ColumnDefinition Width="16" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Padding="8">
            <ListBox ItemsSource="{Binding PreferencesCategories}"
                     SelectedItem="{Binding SelectedPreferencesCategory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </Border>

        <Grid Grid.Column="2">
            <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedPreferencesCategory}" Value="Appearance">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock FontSize="18" FontWeight="SemiBold" Text="Appearance" />
                <TextBlock Margin="0,16,0,6" Foreground="{DynamicResource TextSecondaryBrush}" Text="Theme" />
                <ComboBox Width="180"
                          HorizontalAlignment="Left"
                          ItemsSource="{Binding ThemePreferences}"
                          SelectedItem="{Binding SelectedThemePreference, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedPreferencesCategory}" Value="Processing">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock FontSize="18" FontWeight="SemiBold" Text="Processing" />
                <TextBlock Margin="0,16,0,6" FontWeight="SemiBold" Text="CPU Image Processing" />
                <TextBlock Margin="0,0,0,4" Foreground="{DynamicResource TextSecondaryBrush}" Text="Mode" />
                <ComboBox Width="190" HorizontalAlignment="Left"
                          ItemsSource="{Binding CpuImageProcessingModes}"
                          SelectedItem="{Binding SelectedCpuImageProcessingMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Margin="0,6,0,12" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}"
                           Text="Auto uses most available CPU resources while retaining capacity for system responsiveness." />
                <StackPanel Orientation="Horizontal" IsEnabled="{Binding IsCustomProcessingMode}">
                    <TextBlock Width="130" VerticalAlignment="Center" Text="Maximum workers:" />
                    <TextBox Width="70" Text="{Binding CustomMaximumProcessingWorkers, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                </StackPanel>
                <TextBlock Margin="0,18,0,4">
                    <Run Text="Available logical processors: " /><Run Text="{Binding AvailableLogicalProcessors}" />
                </TextBlock>
                <TextBlock>
                    <Run Text="Effective processing workers: " /><Run Text="{Binding EffectiveProcessingWorkers}" />
                </TextBlock>
            </StackPanel>



            <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedPreferencesCategory}" Value="Player">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock FontSize="18" FontWeight="SemiBold" Text="Player" />
                <TextBlock Margin="0,16,0,4" Text="Oasis Player executable path" Foreground="{DynamicResource TextSecondaryBrush}" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding OasisPlayerExecutablePath, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                    <Button Grid.Column="2" Padding="10,4" Content="Browse" Command="{Binding BrowseOasisPlayerExecutableCommand}" />
                </Grid>
                <TextBlock Margin="0,6,0,0" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}" Text="Build OasisPlayer.exe manually from Unity, then configure that executable here. This user preference is not stored in the Oasis project." />
                <TextBlock Margin="0,18,0,6" Text="Preview Display" Foreground="{DynamicResource TextSecondaryBrush}" />
                <CheckBox Foreground="{DynamicResource TextPrimaryBrush}" Content="Fullscreen" IsChecked="{Binding OasisPlayerFullscreen, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <StackPanel Margin="0,10,0,0" Orientation="Horizontal">
                    <StackPanel>
                        <TextBlock Margin="0,0,0,4" Text="Width" Foreground="{DynamicResource TextSecondaryBrush}" />
                        <TextBox Width="100" Text="{Binding OasisPlayerPreviewWidth, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                    </StackPanel>
                    <StackPanel Margin="12,0,0,0">
                        <TextBlock Margin="0,0,0,4" Text="Height" Foreground="{DynamicResource TextSecondaryBrush}" />
                        <TextBox Width="100" Text="{Binding OasisPlayerPreviewHeight, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                    </StackPanel>
                </StackPanel>
                <TextBlock Margin="0,6,0,0" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}" Text="Windowed preview defaults to 1280 x 800. Fullscreen passes --fullscreen instead of --windowed." />
            </StackPanel>

            <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedPreferencesCategory}" Value="Fabric Emulation">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock FontSize="18" FontWeight="SemiBold" Text="Fabric Emulation" />
                <TextBlock Margin="0,8,0,4" Text="FabricRuntime.dll path" Foreground="{DynamicResource TextSecondaryBrush}" />
                <TextBox Text="{Binding FabricRuntimeLibraryPath, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                <TextBlock Margin="0,8,0,4" Text="JPM System 6 Amber provider DLL path" Foreground="{DynamicResource TextSecondaryBrush}" />
                <TextBox Text="{Binding ProductionAmberLibraryPath, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                <TextBlock Margin="0,8,0,4" Text="Barcrest MPU5 Amber provider DLL path" Foreground="{DynamicResource TextSecondaryBrush}" />
                <TextBox Text="{Binding Mpu5AmberLibraryPath, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                <TextBlock Margin="0,8,0,4" Text="Epoch Amber DLL path" Foreground="{DynamicResource TextSecondaryBrush}" />
                <TextBox Text="{Binding EpochAmberLibraryPath, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                <TextBlock Margin="0,8,0,4" Text="Barcrest MPU3 Amber provider DLL path" Foreground="{DynamicResource TextSecondaryBrush}" />
                <TextBox Text="{Binding Mpu3AmberLibraryPath, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                <TextBlock Margin="0,8,0,4" Text="Maygay M1 Amber provider DLL path" Foreground="{DynamicResource TextSecondaryBrush}" />
                <TextBox Text="{Binding M1AmberLibraryPath, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                <TextBlock Margin="0,8,0,4" Text="Bell-Fruit Scorpion 4 Amber provider DLL path" Foreground="{DynamicResource TextSecondaryBrush}" />
                <TextBox Text="{Binding Scorpion4AmberLibraryPath, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                <TextBlock Margin="0,16,0,4" Text="Audio Buffer Length (ms)" Foreground="{DynamicResource TextSecondaryBrush}" />
                <TextBox Width="120"
                         HorizontalAlignment="Left"
                         Text="{Binding System6AudioBufferLengthMilliseconds, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                <TextBlock Margin="0,4,0,0"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           Text="Playback buffering for Fabric Amber audio. Lower values reduce latency; higher values can help if audio crackles." />
                <TextBlock Margin="0,8,0,0"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           Text="System 6, MPU3, MPU5 and Epoch emulation use FabricRuntime.dll with their explicitly configured Amber provider DLL; Oasis never loads a provider directly." />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
