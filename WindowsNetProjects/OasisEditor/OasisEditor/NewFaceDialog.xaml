<Window x:Class="OasisEditor.NewFaceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Face" Width="460" Height="330" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource PanelBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock FontSize="18" FontWeight="SemiBold" Text="Create Face"/>
        <TextBlock Grid.Row="1" Margin="0,16,0,6" Foreground="{DynamicResource TextSecondaryBrush}" Text="Name"/>
        <TextBox Grid.Row="2" MinHeight="30" Padding="8,4" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Grid.Row="3" Margin="0,16,0,6" Foreground="{DynamicResource TextSecondaryBrush}" Text="Starting Artwork"/>
        <StackPanel Grid.Row="4">
            <RadioButton Margin="0,2" Content="Blank" IsChecked="{Binding IsBlank}"/>
            <RadioButton Margin="0,6,0,0" Content="Image" IsChecked="{Binding IsImage}"/>
            <Grid Margin="22,8,0,0" IsEnabled="{Binding IsImage}">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBox MinHeight="30" Padding="8,4" IsReadOnly="True" Text="{Binding ImagePath}"/>
                <Button Grid.Column="1" Margin="8,0,0,0" Padding="12,5" Content="Choose..." Click="OnChooseImage"/>
            </Grid>
            <TextBlock Margin="22,6,0,0" Foreground="{DynamicResource TextSecondaryBrush}" Text="{Binding ErrorMessage}" TextWrapping="Wrap"/>
        </StackPanel>
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button MinWidth="90" Margin="0,0,8,0" Padding="12,6" IsCancel="True" Content="Cancel"/>
            <Button MinWidth="90" Padding="12,6" IsDefault="True" Content="Create" Click="OnCreate"/>
        </StackPanel>
    </Grid>
</Window>
